services:
  - type: web
    name: scaling-sales-saas
    plan: free
    runtime: node
    buildCommand: npm install
    startCommand: node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_PASSWORD
        value: ScalingSales123!
      - key: OWNER_WHATSAPP
        value: 5516992720495
